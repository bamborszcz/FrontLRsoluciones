export interface Mail {// esto debe ser igual a la tabla de mysql, lo mismo para la otra interfaz y el servicio
    nombre: string;
    mail: string;
    telefono: number;
    localidad: string;
    provincia: string;
    comentario: string;
}
